---
import CollapsibleExp from './CollapsibleExp.tsx';
const { title, company, location, dates, description, skills } = Astro.props;
---

<CollapsibleExp 
  client:load
  title={title}
  company={company}
  location={location}
  dates={dates}
  description={description}
  skills={skills}
/>